/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Remove appDir — not needed
  experimental: {
    // other valid experimental flags if any
  }
};

module.exports = nextConfig;
